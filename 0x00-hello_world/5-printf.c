#include <stdio.h>

/**
 *main - start of the program
 *prinf() - Display text to the std.o
 *Return: terminate program with success
 */
int main(void)
{
	printf("\"with proper grammar, but the outcome is a piece of art,");
	return (0);
}
