#include <stdio.h>
/**
 * main - start the code
 *
 * Return: Terminate the code with success @ 0
 */
int main(void)
{
	int num = 0;

	for (num = 0; num <= 9; num++)
	{
		printf("%d", num);
	}
	printf("\n");
	return (0);
}
