#include "main.h"
/**
 * mul - function that multiplies two integers.
 * @a: first integer taken
 * @b: second interger
 * Return: return multi which is the product of "a" and "b"
 */

int mul(int a, int b)
{
	int product;

	product = a * b;

	return (product);
}
